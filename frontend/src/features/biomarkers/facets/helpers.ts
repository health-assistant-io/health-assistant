/**
 * Re-export of the promoted generic helper. New consumers should import
 * directly from `components/ui/filters` instead.
 */
export { deriveCountedOptions } from '../../../components/ui/filters/deriveCountedOptions';
