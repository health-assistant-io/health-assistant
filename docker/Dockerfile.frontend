FROM node:20-alpine

WORKDIR /app

# Install dependencies (repo-root context: paths are relative to repo root)
COPY frontend/package*.json ./
RUN npm install

# Copy application code
COPY frontend/ .

# Build application
RUN npm run build

# Expose port
EXPOSE 3000

# Run application
CMD ["npm", "run", "preview"]
